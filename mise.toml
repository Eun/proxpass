[tools]
go = "1.25.0"
golangci-lint = "2.12.2"
goreleaser = "2.16.0"

[tasks.build]
description = "Build the proxpass binary"
run = "go build -o proxpass ./cmd/proxpass"

[tasks.lint]
description = "Run golangci-lint"
run = "golangci-lint run ./..."

[tasks.test]
description = "Run tests with coverage"
run = "go test -v -count=1 -cover -coverprofile=coverage.out -covermode=atomic ./..."

[tasks.release]
description = "Run GoReleaser (snapshot)"
run = "goreleaser release --snapshot --clean"

[tasks.demo]
description = "Run the demo server with mock Proxmox"
run = "go run ./cmd/proxpass-demo"

[tasks.clean]
description = "Remove build artifacts"
run = "rm -rf proxpass proxpass-demo dist coverage.out"
